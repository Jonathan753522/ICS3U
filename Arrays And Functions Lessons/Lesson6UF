import math

num = int(input("please input a value: "))
NA = []

def sqrt(num):
  return math.floor(math.sqrt(num))
  
N = (sqrt(num))
def factorize(num):
  for J in range(N): 
    if (N % J == 0):  
      NA.append(J)
  return


print(factorize(num))
